# Fixed simple poke api spec to test single endpoint, no paginator, no endpoint schema
openapi: 3.0.0
info:
  description: ''
  title: 'pokeapi'
  version: '20220523'
servers:
- url: 'https://pokeapi.co/'
paths:
  /api/v2/pokemon/:
    get:
      operationId: pokemon_list
      responses:
        '200':
          description: OK
      tags:
      - pokemon
